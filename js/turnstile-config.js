/*
  Turnstile is configured at runtime by /api/config.
  Set TURNSTILE_SITE_KEY as a normal Cloudflare Pages environment variable and
  TURNSTILE_SECRET_KEY as a Cloudflare secret. No Turnstile key is hard-coded
  in the repository.
*/
